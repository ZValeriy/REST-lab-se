Ты — опытный Python-разработчик, специализирующийся на FastAPI и OpenAPI.  
Твоя задача — сгенерировать исходный код FastAPI-приложения на основе предоставленной OpenAPI-спецификации и Dockerfile.

---

### Входные данные

1️⃣ **Dockerfile:**
```
FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install –no-cache-dir -r requirements.txt
COPY app/ app/
CMD [“uvicorn”, “app.main:app”, “–host”, “0.0.0.0”, “–port”, “8000”]
```

2️⃣ **OpenAPI спецификация:**
(вставь сюда YAML или JSON-описание openAPI, описывающее endpoints, модели и методы)

---

### 🎯 Цель

Сгенерировать исходный код проекта с минимальной, но полной структурой приложения FastAPI,  
реализующей эндпоинты, описанные в OpenAPI.  

Приложение должно:
- работать **без базы данных** (использовать in-memory структуры — словари и списки);
- сохранять бизнес-логику (создание, удаление, поиск, обновление, обработка конфликтов и прочее);
- реализовывать **Basic Authentication** (логин: `basic`, пароль: `basic`);
- иметь чёткое разделение на модули:
  - `models.py` — Pydantic-модели данных;
  - `services.py` — бизнес-логика (операции);
  - `main.py` — контроллеры и точки входа (FastAPI endpoints);
  - `requirements.txt` — зависимости.

---

### 📦 Требования к реализации

#### 1. `requirements.txt`
Добавь только необходимые пакеты:
fastapi
uvicorn
pydantic
python-multipart
(дополнительно можно `httpx` или `requests`, если нужно тестирование или интеграции)

---

#### 2. `models.py`
- Определи все модели данных на основе OpenAPI схем (`BaseModel` из Pydantic).
- Раздели на `Request` и `Response`, если применимо.
- Укажи валидацию (`min_length`, `max_length`, `ge`, `le`, `Field()`).
- Пример:
```python
from pydantic import BaseModel, Field
from typing import Optional, List

class Order(BaseModel):
    id: int
    user_id: int
    amount: float = Field(..., gt=0)
    status: str = Field(..., regex="^(new|paid|canceled)$")

#### 3. services.py
	•	В этом файле должна быть бизнес-логика и in-memory хранилище:
	•	Реализуй функции
	•	Обрабатывай ошибки
#### 4. main.py
	•	Импортируй FastAPI, Depends, HTTPBasic, HTTPBasicCredentials.
	•	Реализуй BasicAuth с логином/паролем basic:basic.
	•	Подключи маршруты, основанные на OpenAPI-описании.
	•	Каждому endpoint-у должна соответствовать функция из services.py.
### Структура итогового проекта 
project/
 ├── Dockerfile
 ├── openapi.yaml
 ├── requirements.txt
 └── app/
     ├── main.py
     ├── models.py
     └── services.py
## Что должна сделать LLM

### Проанализировать OpenAPI-описание и на его основе:
	•	создать модели (models.py);
	•	определить эндпоинты и их параметры (main.py);
	•	реализовать CRUD-операции (services.py).

### Вывести полный, корректно оформленный код для всех файлов:
	•	requirements.txt
	•	models.py
	•	services.py
	•	main.py

### Все файлы должны быть готовы к использованию в Dockerfile, приведённом выше.

### Ответ выдай в формате:
### requirements.txt
```txt
...
```

### models.py
```python
...
```

### services.py
```python
...
```

### main.py
```python
...
```
## Примечание
При генерации:
	•	не реализуй внешние зависимости или базы данных;
	•	endpoints должны строго соответствовать структуре из OpenAPI;
	•	возвращай корректные HTTP-коды и сообщения об ошибках.